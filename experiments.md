# 실험 기록

실험마다 한 줄씩 남긴다. 실패한 실험도 지우지 않는다.
각 실험은 **변수를 하나만** 바꾼다.

대표 지표는 **macro F1**. accuracy 는 기록하지 않는다 (정상 92.7%라 무의미).

| exp_id | 날짜 | 변경점 | 샘플링 | 입력 | macro F1 | 미등장템플릿 F1 | 비고 |
|---|---|---|---|---|---|---|---|
| E0 | 2026-08-26 | 다수 클래스 고정 예측 (하한선) | — | — | 0.2415 | 0.2411 | 계산과 일치 |
| E1 | 2026-08-26 | TF-IDF + 로지스틱 회귀 | none | msg_only | 0.8067 | 0.5192 | 하한선 대비 +0.565 |
| E1c | 2026-08-26 | E1 에서 샘플링만 cap 으로 | cap(2000) | msg_only | **0.9316** | 0.7451 | val 에서 선택. E2 가 넘어야 할 선 |
| E2 | 2026-08-27 | BERT 베이스라인 | none | msg_only | 0.7760 | 0.5144 | **대표 성능. E1c 에 패배.** 미등장 알럿 recall 0 |
| E2c | 2026-08-26 | E1c 에서 모델만 BERT 로 | cap(2000) | msg_only | 0.6829 | 0.4998 | **E1c 에 패배.** 기등장 1.0000 |
| E2i | 2026-08-26 | E2c 에서 체크포인트 선택만 inner-val 미등장으로 | cap(2000) | msg_only | 0.6429 | 0.5049 | 선택은 바뀌었으나 결과 동일 |
| E2w | 2026-08-27 | E2i 에서 클래스 가중치만 끔 | cap(2000) | msg_only | **0.8289** | **0.7330** | 오탐 14,484 → 0. 에폭 1만 유효 |
| E3 | 2026-08-27 | E2 에서 샘플링만 ratio 로 | ratio(3) | msg_only | 0.6410 | 0.5011 | 가중치를 껐는데도 오탐 14,481 재발 |
| E4 | | 샘플링 → balanced | balanced | msg_only | | | 과도한 축소의 부작용 |
| E5 | | 메타 정보 추가 | none | with_meta | | | 참고용, 대표 아님 |
| E6 | | train 완전 중복 줄 제거 | none | msg_only | | | 반복 로그의 영향 |
| E7 | | 로그 코퍼스 MLM 후 파인튜닝 | none | msg_only | | | E2 대비 %p |

---

## 데이터 처리 방침

행을 버리는 결정은 여기에 남긴다. 아래 수치가 바뀌면 그 아래 모든 실험 결과도 다시 봐야 한다.

### 2026-08-25 — 빈 메시지 34,470줄 제외

헤더 9개는 정상이고 메시지만 없는 줄. 파싱 실패가 아니라 유효 레코드다.

| | 값 |
|---|---|
| 건수 | 34,470줄 |
| 제외 전 대비 | 0.7260% (4,747,692 → 4,713,222) |
| normal | 34,470 |
| kernel_mem / kernel_ops / app | 0 / 0 / 0 |

**전부 `normal`.** 남겨두면 "메시지가 비었다 → normal" 이 정확도 100%짜리 지름길
피처가 되어 macro F1 이 부풀려진다. `[EMPTY]` 토큰으로 치환해도 지름길은 그대로다.

버리지 않고 `data/interim/bgl_empty_msg.parquet` 로 분리 저장했다.
정상 비율은 92.67% → 92.61% 로 사실상 변화 없음.

### 2026-08-26 — 시간순 분할 결과와 kernel_mem 표본 문제

행을 버린 결정은 아니지만, **이후 모든 macro F1 의 해석이 이 표에 걸린다.**

| | train | val | test |
|---|---:|---:|---:|
| 기간 | 06-03 ~ 08-30 | 08-30 ~ 11-04 | 11-04 ~ 01-04 |
| 행 | 3,299,255 | 706,995 | 706,972 |
| normal | 3,043,508 (92.25%) | 660,790 (93.46%) | 660,735 (93.46%) |
| kernel_mem | 217,821 (6.60%) | **154** (0.02%) | **100** (0.01%) |
| kernel_ops | 21,026 (0.64%) | 32,287 (4.57%) | 12,956 (1.83%) |
| app | 16,900 (0.51%) | 13,764 (1.95%) | 33,181 (4.69%) |

`kernel_mem` 218,075건 중 **217,821건(99.88%)이 train** 에 있다. 90%가 06-12 ~ 06-14
이틀에 몰린 단일 장애 구간이다. 비율을 어떻게 바꿔도 시간순 단일 절단인 이상 옮겨지지 않는다.

**클래스별 템플릿은 10~24종뿐이고 구간마다 다른 종류가 나온다.**
train 의 `kernel_mem` 99.3% 가 `data TLB error interrupt` + `data storage interrupt` 인데
test 에서 그 둘은 11행뿐이고 대신 `machine check interrupt` / `DDR machine check register`
가 나온다. test 행 기준 미등장 템플릿 비율: normal 75.3%, app 95.4%, kernel_ops 40.6%.

**결정: test/val 은 그대로 둔다.** 정상을 덜어내면 같은 모델의 `kernel_mem` F1 이
0.190 -> 0.842 로 뛴다 (오탐률 0.1% 가정, normal 660,747 -> 10,000). 모델이 아니라
지운 행 수를 재는 숫자가 된다. 층화 축소는 비율이 유지되므로 F1 이 전혀 변하지 않는다
(어느 축소율에서도 0.190) — 표본만 얇아진다. 규칙 2 를 그대로 따른다.

**따라오는 요구사항**
- `evaluate` 는 클래스별 support 와 오탐 건수를 반드시 같이 출력한다. macro F1 하나만
  보면 `kernel_mem` 항이 표본 100건짜리라는 걸 놓친다.
- test 의 `kernel_mem` F1 은 재현율이 아니라 **오탐률로 결정된다.** 정상 660,735건 중
  오탐을 66건(0.01%) 이하로 눌러야 F1 0.65 가 나온다.
- **val 은 test 의 대리 지표가 아니다.** val 의 `app` 은 미등장 템플릿 0.0%, test 는
  95.4%. val macro F1 으로 베스트 체크포인트를 고르면 test 에서 나쁜 모델을 고를 수 있다.
  체크포인트 선택 근거를 E2 에서 다시 볼 것.

### 2026-08-26 — E1 결과와 cap 의 효과

E1(none) 0.8067 -> E1c(cap=2000) **0.9316**. 바꾼 변수는 train 샘플링 하나뿐이고
**선택은 val 에서만 했다** (val 0.9370 -> 0.9925). test 는 선택 후에 한 번 봤다.

| 클래스 | E1 F1 | E1c F1 | recall 변화 |
|---|---:|---:|---|
| normal | 0.9954 | 0.9955 | — |
| kernel_mem | 0.4962 | **0.9950** | 0.3300 -> 0.9900 |
| kernel_ops | 0.7486 | 0.7486 | 0.5982 (변화 없음) |
| app | 0.9865 | 0.9873 | 오탐 64 -> 8 |

**왜 올랐나.** train 의 kernel_mem 217,821줄 중 99.3% 가 `data TLB error interrupt` +
`data storage interrupt` 이고 machine check 계열은 92줄(0.04%)뿐이다. test 의 kernel_mem 은
반대로 machine check / DDR 계열이다. cap 이 앞의 둘을 2,000 으로 깎아 희귀 템플릿의
상대 비중을 40배 올렸다. 모델이 "kernel_mem = TLB/스토리지" 로 배우던 것이 고쳐졌다.

**임계값 보정은 두 실험 모두 가중치 [1,1,1,1] 을 골랐다 — 즉 아무것도 하지 않았다.**
val 에서 app 1.0000 / kernel_ops 0.9838 로 이미 천장이고, 유일한 여지인 kernel_mem 은
가중치를 올리면 정상 660,790행에서 오탐이 쏟아져 F1 이 떨어진다. 좌표상승이 옳게 판단했다.
**남은 문제는 임계값이 아니라 재현율이다.**

**남은 구멍은 kernel_ops 하나.** precision 1.0000 인데 recall 0.5982.
놓친 5,206행의 **100% 가 미등장 템플릿**이고(맞춘 것은 0.7%), 대부분이 한 계열이다.

    5,120행 [미등장] Error receiving packet on tree network, expecting type [NUM]...
            -> 전부 normal 로 예측

test 구간에 새 장애 유형이 등장했고 TF-IDF 는 처음 보는 단어에 할 수 있는 게 없다.
미등장 템플릿만 골라낸 macro F1: E0 0.2411 / E1 0.5192 / E1c 0.7451.
E1c 의 미등장 클래스별 F1 은 normal 0.994 / kernel_mem 0.98 / app 0.987 / **kernel_ops 0.02**.
**E2·E7 이 개선해야 할 대상은 이 한 칸이다.**

운영 관점 오탐(알럿 클래스 오탐 합계)은 8건 / 60.7일 = 하루 0.13건. 예산 5건/일의 2.6%.
정밀도를 재현율과 바꿀 여지가 크지만, val 에 여지가 없어 정직하게 튜닝할 방법이 없다.

### 2026-08-26 — E2c: BERT 가 E1c 에 졌다. 손실 전부가 한 칸에서 나온다

E1c 에서 **모델 하나만** 바꿨다 (TF-IDF+로지스틱회귀 → bert-base-uncased).
샘플링 cap(2000), 입력 msg_only, 클래스 가중치 `w_c = N/(K*n_c)` 모두 동일하다.
베스트 에폭 3, val macro F1 0.9941, 학습 11.4분/에폭.

| 부분집합 | 행 수 | E1c | E2c |
|---|---|---|---|
| test 전체 | 706,972 | **0.9316** | 0.6829 |
| 기등장 템플릿 | 172,357 (24.4%) | 0.9993 | **1.0000** |
| **미등장 템플릿** | 534,615 (75.6%) | **0.7451** | **0.4998** |

**암기는 완벽해졌고 일반화는 나빠졌다.** 기등장 구간 1.0000 은 성능이 아니라 암기의
확인이다 (위기 기록 9번). 판정 기준인 미등장 구간에서 0.7451 → 0.4998 로 내려갔다.

혼동행렬 (행=실제, 열=예측):

| | normal | kernel_mem | kernel_ops | app |
|---|---|---|---|---|
| normal | 645,964 | **14,482** | 2 | 287 |
| kernel_mem | 0 | 100 | 0 | 0 |
| kernel_ops | 5,122 | 0 | 7,791 | 43 |
| app | 816 | 0 | 0 | 32,365 |

**`normal → kernel_mem` 14,482건이 전부다.** E1c 는 같은 칸이 0 이었다.
kernel_ops 놓침(5,122 vs E1c 5,206)과 app 놓침(816 vs 824)은 두 모델이 사실상 같다.
kernel_mem 은 test support 가 100건뿐이라 오탐 14,482건에 precision 0.0069,
F1 0.0136 으로 무너진다. 위기 기록 B 가 예고한 그대로다 — "오탐 66건 이하라야 F1 0.65".

`kernel_mem ↔ kernel_ops` 는 **양방향 모두 0** 이다. 매핑 경계 문제가 아니므로
리매핑 트리거는 걸리지 않았다.

원인은 클래스 가중치가 아니다. `src/baseline.py:32` 의 E1c 도 같은 식을 쓴다.
TF-IDF(min_df=2)는 미등장 템플릿에서 활성 피처가 거의 없어 normal 로 떨어지지만,
BERT 는 서브워드로 일반화해 미등장 정상 로그를 kernel_mem 쪽으로 끌어당긴다.

### 2026-08-26 — E2c 의 신뢰도가 죽었다. unknown 메커니즘이 작동하지 않는다

`infer.conf_threshold: 0.65` 로 저신뢰 예측을 unknown 으로 빼는 설계(SPEC 5-5)를
E2c 예측에 적용해봤다. **임계값을 0.9999 까지 올려도 걸러지는 행이 0 이다.**

| 임계값 | 전체 F1 | 미등장 F1 | unknown 비율 | kernel_mem 오탐 잔존 |
|---|---|---|---|---|
| 0.65 / 0.9 / 0.99 / 0.999 / 0.9999 | 0.6829 | 0.4998 | 0.0% | 14,482 |

- E2c 오분류 **20,752건 전부**의 최대 softmax 확률이 **0.99997 이상**이다.
- `normal → kernel_mem` 오탐 14,482건도 전부 0.99999 이상. 100% 확신하고 100% 틀린다.
- 대조: E1c 는 오분류 최소 신뢰도 0.537, 1-conf 의 95분위 4.9e-03 으로 여유가 있다.
  E2c 는 3.5e-06 이다.

16,945종 고유 템플릿을 19.6배 중복된 332,955행으로 5에폭 돌려 softmax 가 포화됐다
(train loss 0.0002). **`calibrate` 를 E2c 위에 세우면 조정할 신호 자체가 없다.**
E2 계열을 계속 쓰려면 에폭 수·중복도·정칙화를 먼저 손봐야 한다.

### 2026-08-26 — 미등장 템플릿 F1 이 코드 산출물이 됐다

지금까지 이 표의 "미등장템플릿 F1" 열은 애드혹으로 계산돼 재현 절차가 없었다.
`evaluate` 가 `--train-ref` 의 템플릿 집합으로 기등장/미등장을 나눠 채점하고
`metrics.json` 에 `seen` / `unseen` 으로 남긴다. 네 실험을 재채점한 결과가 기존
기록과 정확히 일치했다.

| exp_id | 기등장 172,357행 (24.4%) | 미등장 534,615행 (75.6%) | 전체 |
|---|---|---|---|
| E0 | 0.2431 | 0.2411 | 0.2415 |
| E1 | 0.8988 | 0.5192 | 0.8067 |
| E1c | 0.9993 | **0.7451** | 0.9316 |
| E2c | **1.0000** | 0.4998 | 0.6829 |

기등장은 0.2431 → 1.0000 으로 단조 증가한다. 모델이 좋아진 게 아니라 암기가
완성돼 가는 것이다. 미등장은 0.2411 → 0.5192 → 0.7451 → 0.4998 로 E1c 가 최고다.
**전체 macro F1 은 이 둘을 24 대 76 으로 섞은 값이라 단독으로는 아무것도 말해주지 않는다.**

### 2026-08-26 — train 안에서 정직한 선택 지표를 만든다

E2c 의 실패는 val 로 미리 볼 수 없었고, val 미등장 부분집합도 대안이 못 된다
(kernel_mem 3행 / app 0행, 순위를 거꾸로 매김 — docs/RISKS.md A).

`train --inner-val-frac` 으로 train 뒤쪽을 시간순으로 떼어 검증셋을 만든다.
**경계는 반드시 `unix_ts` 값이다.** 표집된 파일의 행 위치 80% 는 시간의 80% 가 아니다.
`train_cap` 을 행 위치로 자르면 inner-val 미등장의 알럿 3클래스가 전부 0행이 됐다.

`unix_ts` 경계(uncapped train 80% 지점 = 1122166510)로 자른 결과:

| | inner-val 미등장 | (대조) val 미등장 | (대조) test 미등장 |
|---|---|---|---|
| normal | 128,915 | 292,391 | 497,690 |
| kernel_mem | 1,486 | **3** | 25 |
| kernel_ops | 3,352 | 27,453 | 5,259 |
| app | 4,157 | **0** | 31,641 |

4클래스가 모두 살아 있다. inner-train 164,697줄 = 에폭당 5,146스텝.

**한계**: inner-val 미등장의 클래스 비율은 test 미등장과 다르다. 모델 간 순위와
체크포인트 선택에만 쓰고, 절대 수치를 test 추정치로 보고하지 않는다.

### 2026-08-26 — E2i: 체크포인트 선택은 원인이 아니었다

E2c 에서 **선택 기준 하나만** 바꿨다 (`--inner-val-frac 0.20 --select-metric
inner_val_unseen_macro_f1`). inner-train 164,697줄, 에폭당 5,146스텝, 5에폭 30분.

두 기준은 실제로 다른 에폭을 골랐다:

| 에폭 | train loss | val | inner-val | inner-val 미등장 |
|---|---|---|---|---|
| 1 | 0.0952 | 0.7495 | 0.6820 | 0.6524 |
| 2 | 0.0034 | 0.7592 | 0.6758 | 0.6408 |
| 3 | 0.0024 | 0.7486 | 0.6842 | **0.6530** ← 선택됨 |
| 4 | 0.0011 | 0.7654 | 0.6746 | 0.6354 |
| 5 | 0.0008 | **0.7673** ← val 이었다면 | 0.6787 | 0.6410 |

`last.pt` 가 에폭 5 상태라 두 선택지를 test 에서 직접 비교할 수 있었다:

| 체크포인트 | test 전체 | test 미등장 | normal→kernel_mem 오탐 |
|---|---|---|---|
| 에폭 3 (inner-val 미등장이 선택) | 0.6429 | 0.5049 | 14,482 |
| 에폭 5 (val 이었다면) | 0.6429 | 0.5051 | 14,481 |

**차이가 없다.** 에폭 1부터 이미 inner-val 미등장 0.6524 로 포화돼 있었다.
지표는 정직해졌지만 고를 것이 없었다. 체크포인트 선택은 원인이 아니다.

E2c 대비 전체 F1 이 0.6829 → 0.6429 로 내린 것은 kernel_ops recall 이 0.6013 →
0.4162 로 빠졌기 때문이다. 미등장은 0.4998 → 0.5049 로 사실상 같다.

### 2026-08-26 — 오탐 14,482건의 정체: "machine check" + 클래스 가중치

오탐은 흩어져 있지 않았다. **템플릿 2종이 전부이고 14,481건이 하나다.**

    MACHINE CHECK DCR read timeout (mc=e[NUM]x iar [HEX] lr [HEX])   실제 normal
    MACHINE CHECK PLB write IRQ (...)                                실제 normal (1건)

둘 다 train 에 없는 미등장 템플릿이다. BGL 전체에서 "MACHINE CHECK" 를 포함한
40,026행 중 **39,684행(99.1%)이 `-`(normal)** 이고 `KERNMC` 는 342행뿐이다.
BGL 에서 machine check 는 대부분 정보성 메시지다.

BERT 가 그걸 배울 기회는 있었다. 문제는 클래스 가중치가 그 다수를 뒤집는다는 것이다:

| | machine check 행 (normal : kernel_mem) | 가중치 (normal / km) | 가중 손실 기여 |
|---|---|---|---|
| train_cap (E2c·E2i) | 4,196 : 124 = **34 : 1** | 0.271 / 14.838 | 1,137 : 1,840 → **kernel_mem 이 1.62배 우세** |
| train none (E2 예정) | 24,544 : 124 = 198 : 1 | 0.271 / **3.787** | 6,652 : 470 → normal 이 14.2배 우세 |

`cap` 이 normal 쪽만 18,000→2,000, 6,348→2,000 으로 깎고 kernel_mem 124행은
상한에 걸리지 않아 그대로 뒀다. 거기에 `w_c = N/(K*n_c)` 가 kernel_mem 가중치를
14.84 로 올려, **34:1 의 다수가 손실에서는 1:1.62 의 소수가 됐다.**
그래서 미등장 대문자 변종을 만나면 kernel_mem 으로 간다.

E1c 가 이 칸에서 오탐 0 인 것은 더 똑똑해서가 아니다. TF-IDF(min_df=2)는 미등장
템플릿에 활성 피처가 없어 normal 로 떨어질 뿐이다. **일반화하지 못하는 것이
여기서는 정답이었다.**

**예측**: 같은 계산이 `none` 에서는 normal 이 14.2배 우세다. E2(none) 에서는 이
오탐 폭주가 재현되지 않을 가능성이 높다. 검증 방법은 두 가지다 —
cap 에서 `class_weight: false` (30분, 변수 1개) 또는 E2(none) 그대로 (7.5시간, SPEC 대표).

### 2026-08-27 — E2w: 클래스 가중치를 끄니 오탐 14,484건이 0 이 됐다. 단, 에폭 1에서만

E2i 에서 **`--no-class-weight` 하나만** 바꿨다. 베스트 에폭 1, 3에폭에서 조기 종료.

| exp_id | 전체 | 기등장 | **미등장** | normal→kernel_mem 오탐 |
|---|---|---|---|---|
| E1c (넘어야 할 선) | 0.9316 | 0.9993 | **0.7451** | 0 |
| E2i (가중치 켬) | 0.6429 | 0.9511 | 0.5049 | 14,484 |
| **E2w (가중치 끔)** | **0.8289** | 0.8902 | **0.7330** | **0** |

미등장 클래스별로 보면 어디가 고쳐졌는지가 분명하다.

| 미등장 | E1c | E2i | E2w |
|---|---|---|---|
| normal | 0.9940 | 0.9793 | 0.9898 |
| kernel_mem | 0.9796 | **0.0034** | **0.9796** |
| kernel_ops | 0.0200 | 0.0501 | 0.0496 |
| app | 0.9868 | 0.9869 | **0.9130** |

**가설이 맞았다.** `w_c = N/(K*n_c)` 가 kernel_mem 가중치를 14.84 로 올려 34:1 의
machine check 다수를 손실에서 1:1.62 로 뒤집던 것이 원인이다. 가중치를 끄자
kernel_mem 미등장 F1 이 0.0034 → 0.9796 (E1c 와 동일)으로 돌아왔다.

남은 E1c 와의 차이 0.0121 은 **전부 app** 이다 (0.9868 vs 0.9130). 놓친 5,063건은
템플릿 3종뿐이고 전부 `normal` 로 갔다:

    ciod: Error reading message prefix on CioStream socket to [IP]:[NUM], Connection reset by peer   2,256
    ciod: Error reading message prefix on CioStream socket to [IP]:[NUM], Connection timed out       1,991
    ciod: LOGIN chdir([PATH]) failed: Input/output error                                               816

kernel_ops 미등장은 **세 모델 모두 0.02~0.05** 다. E1c 도 recall 0.0101 이다.
이 칸은 클래스 가중치와 무관한 별개의 미해결 문제다.

### 2026-08-27 — 진짜 원인은 하나가 더 있다: 에폭 1을 넘기면 오탐이 돌아온다

E2i 때와 같이 `last.pt`(에폭 3)를 test 에서 직접 채점했다. **가중치를 껐는데도**
에폭 3 에서는 오탐이 그대로 돌아온다.

| E2w 체크포인트 | 전체 | 미등장 | kernel_mem 미등장 | normal→kernel_mem 오탐 |
|---|---|---|---|---|
| 에폭 1 (`best.pt`, 선택됨) | **0.8289** | **0.7330** | 0.9796 | **0** |
| 에폭 3 (`last.pt`) | 0.6225 | 0.4848 | 0.0033 | 14,481 |

**클래스 가중치는 원인의 절반이었다.** 나머지 절반은 학습 시간이다. 가중치를 꺼도
2~3 에폭이면 같은 machine check 오탐을 다시 학습한다 (train loss 에폭 2 에서 0.0009).

무서운 것은 선택 지표가 이 둘을 거의 구별하지 못했다는 점이다:

| 에폭 | train loss | inner-val 미등장 | test 미등장 (실제) |
|---|---|---|---|
| 1 | 0.0557 | **0.6359** | **0.7330** |
| 2 | 0.0009 | 0.6355 | — |
| 3 | 0.0003 | 0.6354 | 0.4848 |

inner-val 미등장의 차이는 **0.0005** 인데 test 미등장은 **0.2482** 벌어진다.
순서는 맞게 매겼지만 칼날 위다. inner-val 미등장의 kernel_mem 은 1,486행뿐이라
이 오탐 모드를 거의 담지 못한다 (docs/RISKS.md A 의 연장).

**다음 실행에 미치는 영향**: 에폭 경계로만 체크포인트를 고르는 지금 구조에서
`none`(3,299,255행, 에폭당 103,102스텝 ≈ 9.9배)은 **최적점이 에폭 1 안쪽에 들어갈
가능성이 높다.** E2 는 `--max-epochs` 를 낮춰 돌리고, 그래도 에폭 1 이 이미 늦으면
에폭 중간 체크포인트가 필요하다.

### 2026-08-27 — E2(none) 대표 성능 0.7760. BERT 는 결국 E1c 를 넘지 못했다

E2w 에서 **샘플링만** `cap(2000)` → `none` (3,299,255행)으로 바꿨다. 클래스 가중치는
E2w 와 같이 끈 채다. 베스트 에폭 1, inner-val 미등장 0.7255.

| 미등장 | normal | kernel_mem | kernel_ops | app | **macro** | 전체 |
|---|---|---|---|---|---|---|
| E1c (넘어야 할 선) | 0.9940 | 0.9796 | 0.0200 | 0.9868 | **0.7451** | 0.9316 |
| E2w (cap) | 0.9898 | 0.9796 | 0.0496 | 0.9130 | **0.7330** | 0.8289 |
| **E2 (none)** | 0.9939 | **0.0769** | **0.0000** | **0.9866** | **0.5144** | 0.7760 |

데이터를 9.9배 늘린 효과가 정확히 갈렸다.

- **app 은 예측대로 고쳐졌다** 0.9130 → 0.9866. E2w 에서 놓치던 `ciod` 템플릿 3종
  5,063건은 cap(2000)이 그 템플릿을 잘라서 생긴 문제가 맞았다.
- **대신 알럿 탐지가 죽었다.** 미등장 kernel_ops 5,259건이 **전부** normal 로 갔고
  (recall 0.0000), kernel_mem 25건 중 24건은 app 으로 갔다 (recall 0.0400).

미등장 혼동행렬 (행=실제):

| | normal | kernel_mem | app |
|---|---|---|---|
| normal | 497,690 | 0 | 0 |
| kernel_mem | 0 | 1 | 24 |
| kernel_ops | 5,259 | 0 | 0 |
| app | 816 | 0 | 30,825 |

**오탐이 0인 대신 recall 도 0이다.** 클래스 가중치를 끈 상태에서 normal 이 92.25%,
중복도가 194.7배라 모델이 "처음 보는 건 normal" 로 수렴했다. E2w(cap)에서는 중복도가
19.6배뿐이라 이 수렴이 덜 일어났다.

**BERT 최고 기록은 E2w 0.7330 이고, E1c 0.7451 에 여전히 진다.** cap 은 알럿을
살리지만 app 템플릿을 자르고, none 은 app 을 살리지만 알럿을 죽인다. 둘을 동시에
만족시키는 샘플링은 아직 없다 — E3(ratio) / E4(balanced) 가 그 사이 지점이다.

### 2026-08-27 — inner-val 미등장 F1 은 실행 간 비교에 쓸 수 없다

E2 를 고를 때 이 지표가 순위를 **거꾸로** 매겼다.

| | inner-val 미등장 (선택 지표) | test 미등장 (실제) |
|---|---|---|
| E2w | 0.6359 | **0.7330** |
| E2 | **0.7255** | 0.5144 |

원인은 정의에 있다. inner-val 의 "미등장"은 **그 실행의 inner-train 기준**이다.
E2w 의 inner-train 은 cap 된 2,914종, E2 의 inner-train 은 훨씬 많은 종이라
같은 이름의 두 지표가 서로 다른 행 집합을 재고 있다. 값이 커진 것은 모델이
좋아져서가 아니라 미등장 집합이 쉬워졌기 때문이다.

**용도를 좁힌다: 한 실행 안에서 체크포인트를 고르는 데만 쓴다.** 학습 입력이 다른
두 실행의 우열은 이 값으로 판단하지 않는다. `docs/RISKS.md` A 에 추가한다.

### 2026-08-27 — E3(ratio) 0.6410. 샘플링이 클래스 가중치와 같은 사고를 냈다

E2 에서 **샘플링만** `none` → `ratio(3)` 으로 바꿨다. 클래스 가중치는 계속 꺼둔 채다.
`sample` 은 정상만 3,043,508 → 767,241 로 깎고 알럿 3클래스는 한 행도 건드리지 않았다
(val/test 해시 변경 없음). 노린 것은 `cap` 이 자르던 `ciod` app 템플릿을 온전히 남기는 것.

**app 은 노린 대로 됐다** — 미등장 app F1 0.9869 (E2w 0.9130). 그런데 `normal →
kernel_mem` 오탐 14,481건이 돌아왔다. **클래스 가중치를 껐는데도.**

학습셋 구성을 뽑아보면 이유가 하나로 모인다.

| 실험 | 샘플링 | 학습셋 kernel_mem 비중 | 클래스 가중치 | 실효 비중 | normal→km 오탐 | 미등장 |
|---|---|---|---|---|---|---|
| E2c / E2i | cap(2000) | 1.68% | ×14.84 | ~25% | 14,482 / 14,484 | 0.4998 / 0.5049 |
| **E2w (에폭 1)** | cap(2000) | 1.68% | 끔 | **1.68%** | **0** | **0.7330** |
| E2w (에폭 3) | cap(2000) | 1.68% | 끔 | 1.68% | 14,481 | 0.4848 |
| E2 | none | 6.60% | 끔 | 6.60% | 0 | 0.5144 |
| **E3** | ratio(3) | **21.29%** | 끔 | **21.29%** | **14,481** | 0.5011 |

`ratio` 가 정상만 74.8% 날려 kernel_mem 비중을 6.60% → **21.29%** 로 올렸다.
`w_c = N/(K*n_c)` 가 하던 일을 샘플링이 그대로 대신했다. **방식이 달랐을 뿐 같은 사고다.**

machine check 구간의 균형도 같이 무너진다:

| 학습셋 | machine check normal : kernel_mem |
|---|---|
| none | 24,544 : 124 = 197.9 : 1 |
| cap(2000) | 4,196 : 124 = 33.8 : 1 |
| ratio(3) | 6,178 : 124 = 49.8 : 1 |

**발화 조건은 두 개다.** (1) kernel_mem 실효 비중이 높거나 (임계는 6.60% 와 21.29%
사이), (2) 비중이 낮아도 2에폭 이상 돌리거나. E2w 에폭 1 은 둘 다 피한 유일한 지점이고,
그래서 칼날 위다.

### 2026-08-27 — 오탐 14,481 은 미등장 템플릿 **1종**이다

E2c 14,482 / E2i 14,484 / E2w 에폭3 14,481 / E3 14,481. 숫자가 계속 같은 이유는
흩어진 오탐이 아니라 `MACHINE CHECK DCR read timeout (mc=e[NUM]x iar [HEX] lr [HEX])`
**한 템플릿의 전부 아니면 전무**이기 때문이다.

kernel_mem 의 test support 는 100건뿐이다 (위기 기록 B). 그래서 이 템플릿 1종을
kernel_mem 으로 보내는 순간 precision 이 0.0069 로 떨어지고, **미등장 macro F1 이
0.73 ↔ 0.50 으로 통째로 흔들린다.** 모델의 표현력이 아니라 사전확률 한 칸의 문제다.

**샘플링 축은 여기서 닫는다.** cap / none / ratio 세 지점을 다 찍었고 어느 것도
세 클래스를 동시에 살리지 못했다 — cap 은 app 템플릿을 자르고, none 은 알럿 recall 을
0 으로 만들고, ratio 는 kernel_mem 오탐을 부른다. E4(balanced)는 kernel_mem 비중을
E3 보다 더 올리므로 돌리지 않아도 결과가 예측된다.

**BERT 최종 성적: 5판(E2c/E2i/E2w/E2/E3) 전패.** 최고가 E2w 0.7330 이고 E1c 0.7451 에
0.0121 못 미친다. 대표 모델은 E1c 로 확정한다.

### 2026-08-27 — 평시 기준선에서 06-12~06-14 장애 3일을 뺀다

`calibrate` 의 `class_ratio_mean/std` 계산에서만 뺀다. **학습 데이터는 건드리지 않는다** —
train.parquet 은 그대로고 E0~E3 의 수치는 전혀 영향받지 않는다.

| | 값 |
|---|---|
| 구간 | 2005-06-12 00:00 ~ 2005-06-15 00:00 |
| 1분 버킷 | 680개 / 11,293개 (**6.0%**) |
| normal | 2,148 |
| kernel_mem | **215,673** (train kernel_mem 217,821 의 **99.0%**) |
| kernel_ops | 0 |
| app | 0 |

**넣으면 감지기가 kernel_mem 을 사실상 못 잡는다.** 이틀짜리 장애가 mean 과 std 를
동시에 끌어올려 `mean + 3σ` 임계가 이렇게 된다:

| 클래스 | 장애 포함 임계 | **장애 제외 임계** |
|---|---|---|
| kernel_mem | 0.6068 | **0.0252** |
| kernel_ops | 0.3171 | 0.3969 |
| app | 0.1965 | 0.2552 |

포함하면 한 구간의 **61%가 kernel_mem 이어야** warning 이 뜬다. 20%짜리 진짜 장애는
조용히 지나간다. SPEC 5-4 가 "장애 상태를 평시로 학습하면 안 된다"고 경고한 바로 그 일이
추론 시점이 아니라 calibrate 시점에 일어난다.

`infer.baseline_exclude` 로 설정에 두고 되돌릴 수 있게 했다. kernel_ops / app 임계가
오히려 조금 올라가는 것은 그 두 클래스는 이 구간에 0행이라, 분모(버킷 수)만 줄어서다.

버킷 로그 수가 `infer.min_count`(5) 미만인 것도 기준선에서 뺀다. BGL 은 하루 130분만
로그가 있고 버킷당 median 8건이라, 8건짜리 버킷의 비율은 0 아니면 0.125 로만 나와
분포를 왜곡한다. 11,293 → 장애 680 제외 → 저건수 제외 → **기준선 5,768개**.

### 2026-08-27 — infer 검증. 트래픽 감지기가 BGL 에서 두 번 틀렸다

SPEC 5-6 대로 val/test 구간을 **원본 로그로** `infer` 에 통과시켰다 (파싱 실패 0.00%).
처음 돌린 결과가 하루 **18.9건**(val) / **20.9건**(test)으로 예산 5건/일을 크게 넘었다.

규칙별로 쪼개니 원인이 하나였다.

| 규칙 | val (건/일) | test (건/일) |
|---|---|---|
| **트래픽 급증** | **17.9** | **18.5** |
| app 비율 | 0.8 | 2.6 |
| unknown 비율 | 0.6 | 1.4 |
| kernel_mem 비율 | 0.5 | 0.1 |
| kernel_ops 비율 | 0.2 | 0.3 |

분류기 규칙만으로는 val 2.1 / test 4.4 로 예산 안이다. **문제는 트래픽 쪽 하나다.**

**틀린 것 1 — z 에 상한이 있었다.** 잔차의 이동 표준편차를 현재 잔차까지 포함해
계산하고 있었다. 큰 스파이크가 자기 분모를 같이 키워 z 가 자기 자신을 누른다.
val 에서 z 최대가 **5.6** 이라, 임계를 6 으로 올리면 어떤 급증도 영영 안 잡혔다.

    |z|>3  17.9건/일   |z|>4  11.9   |z|>5  7.2   |z|>6  0.0   |z|>8  0.0

기준선과 척도를 모두 **과거 잔차로만**(shift) 잡도록 고쳤다.

**틀린 것 2 — BGL 에는 EWMA z 가 성립하지 않는 구간이 대부분이다.** val 96,126분 중
로그가 있는 분이 9,530개(**90%가 0건**)다. 조용한 구간에서 잔차 표준편차가 0 에 붙어
z 가 폭주한다(고친 뒤 z 최대 2,809,495). 비율 판정의 `min_count` 와 같은 취지로
`traffic.min_baseline` 가드를 넣었다. **val 에서만** 고른 값이다:

| 가드 | 판정 대상 구간 | \|z\|>3 | \|z\|>4 | \|z\|>5 |
|---|---|---|---|---|
| 없음 | 9,529 | 20.0 | 16.0 | 13.3 |
| 5 | 3,153 | 4.6 | 3.2 | 2.2 |
| **20** | **1,178** | **1.5** | 0.7 | 0.3 |
| 50 | 482 | 0.4 | 0.1 | 0.1 |

SPEC 의 `z > 3` 은 그대로 두고 가드만 20 으로 넣었다. 트래픽 감지기는 NASA HTTP
로그(고른 요청률)를 기준으로 설계된 것이라, BGL 처럼 90%가 빈 구간인 신호에는
가드 없이 쓸 수 없다.

**고친 뒤 (판정은 val 에서, test 는 한 번만 봤다)**

| | 알림/일 | 탐지/일 | **오탐/일** | 재현율 |
|---|---|---|---|---|
| val | 3.3 | 1.6 | **1.7** | 23.3% |
| test | 5.7 | 3.4 | **2.3** | 53.8% |

**오탐은 둘 다 목표 5건/일 안이다.** 재현율이 낮은 것은 그 예산의 대가다.
여기서 "진짜 이상 구간"은 알럿 라벨이 1행이라도 있는 버킷이라 기준이 매우 느슨하다 —
1,000줄짜리 구간의 알럿 1행까지 세므로 실제 운영 가치보다 재현율을 낮게 잡는다.

### 2026-08-27 — 두 모델을 같은 추론 파이프라인에 세웠다. E1c 가 운영 지표에서도 이긴다

`baseline` 이 모델을 저장하게 고친 뒤 E1c 를 재실행했다. **재채점 결과가 기존 기록과
정확히 일치했다** (전체 0.9316 / 기등장 0.9993 / 미등장 0.7451). 저장 코드는 아무것도
바꾸지 않았다.

같은 `calibrate` → `infer` 를 두 모델에 그대로 돌린 결과 (원본 로그 통과, 판정은 val,
test 는 한 번만 봤다):

| | 알림/일 | 탐지/일 | **오탐/일** | 재현율 |
|---|---|---|---|---|
| **E1c** val | 4.8 | **3.6** | **1.3** | **51.3%** |
| **E1c** test | 5.1 | **3.7** | **1.4** | **59.3%** |
| E2w val | 3.3 | 1.6 | 1.7 | 23.3% |
| E2w test | 5.7 | 3.4 | 2.3 | 53.8% |

**E1c 가 더 많이 잡고 덜 틀린다.** 분류 지표(미등장 macro F1 0.7451 vs 0.7330)와
운영 지표가 같은 방향을 가리킨다. 두 모델 모두 오탐은 목표 5건/일 안이다.

**E1c 에서는 `unknown` 메커니즘이 꺼진다.** val 에서 macro precision 이 임계 0.0 부터
0.95 까지 **0.9980 으로 평평**해서, 예측을 버려도 얻는 게 없다. 규칙이 동점일 때
unknown 이 적은 쪽을 고르므로 0.0 이 뽑힌다.

| 임계 | 0.0 | 0.9 | 0.95 | 0.99 | 0.999 |
|---|---|---|---|---|---|
| macro P | 0.9980 | 0.9980 | 0.9980 | 0.9979 | 0.9974 |
| unknown | 0.00% | 0.02% | 0.04% | 6.24% | 40.53% |

이건 val 이 test 의 대리 지표가 아니라는 문제의 연장이다 (`docs/RISKS.md` A) — val 에서
E1c 는 macro F1 0.9925 로 거의 완벽해서 고칠 것이 안 보인다. **결과적으로 SPEC 5-5 의
`holdout_rare` 271건이 `unknown` 으로 나오는지 확인하는 검증은 E1c 로는 통과할 수 없다.**
E2w 는 임계 0.999 가 뽑혀 unknown 이 동작한다 (test 29,544줄).
