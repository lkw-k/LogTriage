# 실험 기록

실험마다 한 줄씩 남긴다. 실패한 실험도 지우지 않는다.
각 실험은 **변수를 하나만** 바꾼다.

대표 지표는 **macro F1**. accuracy 는 기록하지 않는다 (정상 92.7%라 무의미).

| exp_id | 날짜 | 변경점 | 샘플링 | 입력 | macro F1 | 미등장템플릿 F1 | 비고 |
|---|---|---|---|---|---|---|---|
| E0 | | 다수 클래스 고정 예측 (하한선) | — | — | | | 0.25 근처 예상 |
| E1 | | TF-IDF + 로지스틱 회귀 | none | msg_only | | | E2가 이걸 못 이기면 문제 |
| E2 | | BERT 베이스라인 | none | msg_only | | | **대표 성능** |
| E3 | | 샘플링 → ratio | ratio | msg_only | | | 정상 축소 효과 |
| E4 | | 샘플링 → balanced | balanced | msg_only | | | 과도한 축소의 부작용 |
| E5 | | 메타 정보 추가 | none | with_meta | | | 참고용, 대표 아님 |
| E6 | | train 완전 중복 줄 제거 | none | msg_only | | | 반복 로그의 영향 |
| E7 | | 로그 코퍼스 MLM 후 파인튜닝 | none | msg_only | | | E2 대비 %p |

---

## 데이터 처리 방침

행을 버리는 결정은 여기에 남긴다. 아래 수치가 바뀌면 그 아래 모든 실험 결과도 다시 봐야 한다.

### 2026-08-25 — 빈 메시지 34,470줄 제외

헤더 9개는 정상이고 메시지만 없는 줄. 파싱 실패가 아니라 유효 레코드다.

| | 값 |
|---|---|
| 건수 | 34,470줄 |
| 제외 전 대비 | 0.7260% (4,747,692 → 4,713,222) |
| normal | 34,470 |
| kernel_mem / kernel_ops / app | 0 / 0 / 0 |

**전부 `normal`.** 남겨두면 "메시지가 비었다 → normal" 이 정확도 100%짜리 지름길
피처가 되어 macro F1 이 부풀려진다. `[EMPTY]` 토큰으로 치환해도 지름길은 그대로다.

버리지 않고 `data/interim/bgl_empty_msg.parquet` 로 분리 저장했다.
정상 비율은 92.67% → 92.61% 로 사실상 변화 없음.
